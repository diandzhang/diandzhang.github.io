language: node_js
node_js: stable

# S: Build Lifecycle
install:
  - npm install


#before_script:
 # - npm install -g gulp
cache:
  directories:
    - node_modules
script:
  - pwd
  - ls -l
  - chmod 777 node_modules
  - chmod 777 /home/travis/.travis/job_stages
  - ls -l  
  - ls -l node_modules
  - hexo g
  - ls
after_script:
  - cd ./public
  - git init
  - git config user.name "diandzhang"
  - git config user.email "diandzhang@gmail.com"
  - git add .
  - git commit -m "Update docs"
  - git push --force --quiet "https://${GH_TOKEN}@${GH_REF}" master:master
# E: Build LifeCycle

branches:
  only:
    - hexo
env:
 global:
   - GH_REF: github.com/diandzhang/diandzhang.github.io.git